console.log("Seed data is built into the local development store.");
console.log("Production Prisma seeding should create the same roles, employees, policies, projects, tasks, and sample attendance described in README.md.");
